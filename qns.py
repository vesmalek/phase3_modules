# Q1. Import the math module and use it to:
#     a) Print the value of pi
#     b) Round 7.5 down using math.floor
#     c) Find the square root of 144

# Q2. From the datetime module, import only datetime
#     Print the current date and time
#     Then print just the current year by accessing .year on the result

# Q3. Create a file called helpers.py with three functions:
#     - calculate_discount(price, percent) → returns discounted price
#     - greet_user(username) → returns "Welcome, {username}!"
#     - is_valid_password(password) → returns True if len >= 8, False otherwise
#
#     In a separate main.py, import all three functions from helpers.py
#     and test each one with sample inputs

# Q4. Generate a random token using random and string modules
#     Write a function called generate_token(length=10) that returns
#     a random mix of letters and digits of the given length
#     Call it twice — once with default length, once with length=20

# Q5. Using the json module:
#     Create a user dict with at least 4 keys
#     Convert it to a JSON string and print it
#     Convert it back to a dict and access one key from the result